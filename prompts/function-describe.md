You are analyzing functions from a codebase. For each function listed below, provide a short description of what it does.

## Rules

- Maximum 15 words per description
- Start with a verb (e.g. "Validates user input", "Sends HTTP request")
- Do NOT start with "This function" or "This method"
- If the function name is already self-explanatory (e.g. `getUserId`, `isEmpty`, `toString`), respond with "skip"
- If the function is a simple getter/setter (one line, just returns or sets a field), respond with "skip"
- Output format: one line per function, `function_name: description`

## Functions to analyze

{{FUNCTIONS}}

## Output format

function_name_1: Short description starting with verb
function_name_2: Another short description
function_name_3: skip
